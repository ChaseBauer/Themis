apiVersion: v1
kind: Service
metadata:
  name: {{ include "themis.backend.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "themis.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "themis.backend.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
